import sys

def get_device_list():
    sys.exit("OS not supported")

def mount(device):
    sys.exit("OS not supported")

def get_mount_point(device):
    sys.exit("OS not supported")

def unmount(device):
    sys.exit("OS not supported")

def rpi_imager_path():
    sys.exit("OS not supported")

def get_wlan_information():
    sys.exit("OS not supported")